<% @barbers.each do |b| %>
<br>
<%= link_to (image_tag b.avatar.url(:medium), class: "img-square", width: 300, height: 300), barber_path(b) %>
<%= b.name %> 
<!-- image_tag b.avatar.url(:medium). This would allow us to link our image  -->
<% end %>

